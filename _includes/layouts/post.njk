---
layout: layouts/base.njk
templateClass: tmpl-post
---
<h1>{{ title }}</h1>

{# Use the date from data cascade (set by the generator) #}
<time datetime="{{ date | htmlDateString }}">{{ date | readableDate }}</time>

{%- for tag in tags | filterTagList -%}
  {%- set tagUrl %}/tags/{{ tag | slug }}/{% endset -%}
  <a href="{{ tagUrl | url }}" class="post-tag">{{ tag }}</a>
{%- endfor %}

{{ post.fields.body | richtext | safe }}

{# Prefer our sorted 'posts' collection; fallback to tag collection #}
{%- set coll = collections.posts or collections['post'] -%}
{%- if coll and (coll | length) > 0 -%}
  {%- set nextPost = coll | getNextCollectionItem(page) -%}
  {%- set previousPost = coll | getPreviousCollectionItem(page) -%}
  {%- if nextPost or previousPost -%}
    <hr>
    <ul>
      {%- if nextPost %}<li>Next: <a href="{{ nextPost.url | url }}">{{ nextPost.data.title }}</a></li>{% endif %}
      {%- if previousPost %}<li>Previous: <a href="{{ previousPost.url | url }}">{{ previousPost.data.title }}</a></li>{% endif %}
    </ul>
  {%- endif -%}
{%- endif -%}

