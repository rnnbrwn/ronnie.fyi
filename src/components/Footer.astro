---
const currentYear = new Date().getFullYear();
---

<footer>
	<p><a href='/'>ronnie.fyi</a> online from 2001 - {currentYear} in some guise or other.</p>
	<nav>
		<a href='/uses'>Uses</a>
		<a href='/changelog'>Changelog</a>
	</nav>
</footer>

<style lang='scss'>
	@use 'sass:map';
	@use '../styles/abstracts/breakpoints';
	@use '../styles/abstracts/spacing';
	@use '../styles/abstracts/colors';
	@use '../styles/abstracts/layout';
	@use '../styles/abstracts/typography';

	footer {
		margin-top: auto;
		padding: spacing.$space-6 spacing.$space-4;
		border-top: 1px solid map.get(colors.$dark-tones, 'tone-5');
		background-color: map.get(colors.$light-shades, 'shade-1');
		display: flex;
		justify-content: space-between;
		align-items: flex-end;

		@media (min-width: breakpoints.$md) {
			padding: spacing.$space-6 spacing.$space-24 spacing.$space-8;
		}

		nav {
			margin-top: 0;
			display: flex;
			gap: spacing.$space-4;

			a {
				font-size: typography.$font-size-md;
				@include layout.body-link-full;
			}
		}

		p {
			margin: 0;
			color: colors.$dark;
			font-weight: typography.$weight-regular;
			font-size: typography.$font-size-md;

			a {
				@include layout.body-link-full;
			}

			:global(html[data-theme='dark']) & {
				color: map.get(colors.$light-tones, 'tone-2');
			}
		}
	}
</style>
