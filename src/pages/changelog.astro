---
import Base from '../layouts/Base.astro';
import { getPage } from '../utils/wordpress';

const buildDate = new Date().toISOString().replace('T', ' ').split('.')[0] + ' UTC';
const page = await getPage('changelog');
---

<Base title='Changelog' description='A log of notable updates and changes to ronnie.fyi.'>
	<h1>Changelog</h1>
	<div class='wp-content'><Fragment set:html={page?.content} /></div>
	<hr />
	<pre>last build: {buildDate}</pre>
</Base>

<style lang='scss'>
	@use '../styles/abstracts/layout';
	@use '../styles/abstracts/spacing';
	@use '../styles/abstracts/typography';

	h1 {
		@include typography.h1-page-title;
		@include layout.h1-saffron-underlay;
	}

	.wp-content :global(h2) {
		@include typography.h2-section-label;
	}

	.wp-content :global(a) {
		@include layout.body-link;

		:global(html[data-theme='dark']) & {
			@include layout.body-link-on-dark;
		}
	}

	.wp-content :global(ul) {
		max-width: layout.$prose-width;
	}

	hr {
		margin: spacing.$space-12 0;
	}
</style>
